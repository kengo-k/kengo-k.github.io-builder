name: deploy
on:
  push:

jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: node:19.1.0-bullseye
    steps:
      - name: build site
        run: |
          git clone https://github.com/kengo-k/blog-builder.git
          cd blog-builder
          yarn
          yarn build
          ls -la
      - name: deploy site
        run: |
          ls -la
